// NOTE: Keep this class at the end of the file (and do not add a trailing newline before EOF)
class Person {
  var name: String

  init(name: String = "Marcin") {
    self.name = name
  }
} // class Person